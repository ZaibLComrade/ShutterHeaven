import {ThemedText} from "@/components/ThemedText";
import {ScrollView} from "react-native"

const Dashboard = () => {
	return <ScrollView>
		<ThemedText>This is dashboard</ThemedText>
	</ScrollView>
}

export default Dashboard;
